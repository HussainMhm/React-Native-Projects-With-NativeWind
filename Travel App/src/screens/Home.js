import { View, Text, SafeAreaView } from "react-native";

function Home(props) {
    return (
        <SafeAreaView className="flex-1 justify-center items-center">
            <Text className="text-4xl font-bold text-blue-800">Home</Text>
        </SafeAreaView>
    );
}

export default Home;
