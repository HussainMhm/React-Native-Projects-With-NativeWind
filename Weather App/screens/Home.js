import { View, Text, SafeAreaView } from "react-native";

function Home(props) {
    return (
        <SafeAreaView>
            <Text>Home</Text>
        </SafeAreaView>
    );
}

export default Home;
